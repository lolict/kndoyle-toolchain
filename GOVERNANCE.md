# GOVERNANCE - 仓库治理公约（任何 AI 接手任何仓库前必读）

本公约适用所有参与本仓库网络维护的 AI 与人。无论你在哪个仓库工作，
先读完本公约，再动手。它总结了在"多仓库、按需索取、打包分发"场景下
必须避免的风险，以及每项风险的防治手段。

## 一、最高原则：一代码只在一处

- **单一事实源**：每个仓库只维护自己归属的代码，不复制其他仓库的代码。
- **镜像 = 整仓备份**，不是内容复制。镜像用 `git push --mirror` 保持字节级一致。
- **重复代码是万恶之源**：打包冲突、内容缺失、内存膨胀，根源都是"同一份代码
  存在多份副本"。缺功能就按需索取，不缺就不要。

## 二、按需索取（vendor）——缺什么拉什么

缺其他仓库的重要功能时，**不整仓搬运**，只拉需要的路径：

```bash
bash pull.sh <仓库名> <路径...>
# 例: bash pull.sh yuan-ji-jiu-can hunyuan-rs
```

产物进 `vendor/<来源仓库>/<路径>/`，并满足：
- **独立目录**：一眼看出是哪个仓库来的，不混合搅拌
- **来源标注**：`SOURCE.md` 记录仓库 / commit / 日期，未来可核对版本
- **只拉缺的**：不缺的不拉；拉进来的是独立可用模块

## 三、支撑完整性检查——防止"漏掉支撑，代码失去用意"

局部拉取可能漏掉被拉代码依赖的支撑文件。**每次拉取后必须检查**：

```bash
python3 checkdeps.py          # 检查全部 vendor
python3 checkdeps.py <仓库名>  # 检查指定 vendor
```

检查项：
1. **支撑完整性**：`mod`/`import`/`include`/`require` 声明的每个模块文件
   是否都已拉取（缺支撑 = 代码失去用意 = 不能用）
2. **重复状态**：sha256 全库比对，拉取代码是否与仓库其他内容重复

两项都 PASS 才可打包。FAIL 即停止：缺支撑就补支撑，有重复就删副本。

## 四、兼容补丁——上游有 bug 时的正解

若检测发现**上游代码自身有问题**（如 `__init__.py` 引用不存在的符号），
用独立补丁修复，不直接改源仓库风格：

```bash
bash apply_patch.sh <仓库名>   # 应用补丁并验证可运行
```

- 补丁放 `patches/`，应用后进 vendor 并记录于 SOURCE.md
- vendor 保持来源纯净、可再拉取；重拉后重新应用补丁
- 验证到"能导入 + 能运算"，而非仅静态检查

## 五、打包治理——防冲突、防缺失、防重复

```bash
bash package.sh   # 一键打包 → dist/kndoyle-<版本>.tar.gz
```

- 打包只基于主仓库（单一事实源），产物完整、无重复、无缺失
- 打包产物含 sha256 清单，逐文件校验一致
- vendor 随包携带，来源清晰（SOURCE.md）

## 六、未知仓库不碰

未读过 README/结构、读不到（404/私有）的仓库视为未知：
- 不往里面推代码
- 不把它当参考实现猜测用途
- 推仓库前先读该仓库 README 与本公约

## 七、每次动手前的自检清单

- [ ] 要动的代码属于哪个仓库？这个仓库是干什么的？读过 README 了吗？
- [ ] 缺功能时，用按需索取而不是复制粘贴？
- [ ] 拉取后跑过支撑完整性 + 重复检测？
- [ ] 上游有 bug 时，用补丁机制而不是改源码？
- [ ] 打包前确认"一代码只在一处"？
- [ ] 提交后镜像用 `--mirror` 或整仓同步，保持一致性？

---

*本公约是"思想准备"。把它放在每个仓库里，让每个接手的 AI 都读到，
就能避免同样的风险。规则本身是一份文档，不构成代码重复。*
